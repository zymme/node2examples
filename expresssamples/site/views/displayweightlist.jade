extends layout

block content
 table
  caption Weight Information
   if weights.length
    tr
     th ID
     th Name
     th Weight
     th Day
     th
     th
    each weight in weights
     tr
      td #{weight.id}
      td #{weight.name}
      td #{weight.weight}
      td #{weight.day}
      td
       a(href='/weights/#{weight.id}/delete') Delete
      td
       a(href='/weights/#{weight.id}/edit') Edit
 br
 table
  tr
   td
    a(href='/weights/add/amount') Add Amount
   